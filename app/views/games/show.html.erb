<h1>Welcome Player</h1>

<div class="panel panel-default">
  <div class="panel-body">
    Your balance: <%= @player.balance %>
  </div>
</div>

<h4>How it works</h4>

<ol>
  <li>Input some value into the "Bet Input"</li>
  <li>Input a bet amount</li>
  <li>Click "Place bet"</li>
</ol>

<h4>Determining a winner</h4>
<p>
  Your input text is SHA'd using a server secret. <br />
  The first 4 hex bytes are converted to an integer to create your "Lucky Number". <br />
  A random roll is made between 0 - 65, 535. <br />
  If your "Lucky Number" is less than the roll, you win!
</p>
<br />

<% if @invalid_bet %>
  <p class='text-danger'>
    Invalid bet!
  </p>
<% end %>

<%= form_tag [:bet, :games], role: 'form', class: 'form-horizontal' do %>
  <div class="form-group">
    <label for="exampleInputEmail1" class="col-sm-2">Bet Input:</label>
    <div class='col-sm-10'>
      <%= text_field_tag :bet_input, nil, class: 'form-control', placeholder: 'Bet Input', required: true %>
    </div>
  </div>

  <div class="form-group">
    <label for="exampleInputEmail1" class="col-sm-2">Bet:</label>
    <div class='col-sm-3'>
      <%= text_field_tag :amount, nil, class: 'form-control', placeholder: 'Bet amount', required: true %>
    </div>
  </div>
  <div class="form-group">
    <div class='col-sm-offset-2 col-sm-3'>
      <%= submit_tag "Place bet", class: 'btn btn-success' %>
    </div>
  </div>
<% end %>


<% if @result_hex %>
  <table class='table'>
    <tbody>
      <tr>
        <td>Bet</td>
        <td><%= @bet %></td>
      </tr>
      <tr>
        <td>Input Text</td>
        <td><%= params[:bet_input] %></td>
      </tr>
      <tr>
        <td>Salt</td>
        <td><%= @secret %></td>
      </tr>
      <tr>
        <td>Hex</td>
        <td><%= @result_hex %></td>
      </tr>
      <tr>
        <td>Lucky Number</td>
        <td><%= @result_int %></td>
      </tr>
      <tr>
        <td>Roll</td>
        <td><%= @roll %></td>
      </tr>
      <tr>
        <td>Result</td>
        <td><%= @message %></td>
      </tr>
    </tbody>
  </table>
<% end %>
